import zipfile
import os

lna_file = "workflow.lna"
extract_dir = "extracted_lna"

# Create output directory
os.makedirs(extract_dir, exist_ok=True)

# Extract contents
with zipfile.ZipFile(lna_file, 'r') as zip_ref:
    zip_ref.extractall(extract_dir)

print("Extraction completed!")
