def list_dir(self, path: Path) -> list[Path]:
    """Implement directory listing for local storage"""

    path = self._dir / path

    if not path.exists():
        return []

    items = path.glob("*")
    items = (f.relative_to(self._dir) for f in items)

    return list(sorted(items))
