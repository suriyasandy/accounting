# -------------------------
# Root Cause
# -------------------------
root_cause = None

try:

    if len(contrib) > 0:

        # safe feature length
        safe_len = min(len(contrib), len(self._feature_keys))

        contrib_slice = contrib[:safe_len]

        idx = int(np.argmax(contrib_slice))

        root_cause = self._feature_keys[idx]

except Exception as e:
    logger.warning(f"Root cause calculation failed: {e}")
